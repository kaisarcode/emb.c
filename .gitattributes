# Large generated model data treated as binary to avoid heavy diffs
model.c -text

# Source files and documentation
*.c text
*.h text
*.cpp text
*.md text
# Shell scripts with fixed LF line endings
*.sh text eol=lf
# GGML library structure
ggml/include/** text
ggml/src/** text
model.c filter=lfs diff=lfs merge=lfs -text
